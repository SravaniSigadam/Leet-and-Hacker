class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        ans = []
        nums1 = nums.copy()
        for i in nums:
            ans.append(i)
        for j in nums1:
            ans.append(j)
        return ans
      
